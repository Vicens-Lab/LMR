# assets

Figures used in the README (all from the paper):

- `cross_corpus.png`      one model, wildly different scores across public benchmarks (the problem)
- `trifecta.png`          the SHIFU Trifecta (three-axis evaluation)
- `frontier.png`          the LMR family frontier (compact models match/beat larger ones)
- `dataset.png`           SHIFU-Corpus construction funnel + per-split composition
- `finetune_workflow.png` MLM pretraining -> parameter-efficient adaptation -> staged curriculum

Optional: to add a project logo as the README header, save a PNG here (e.g. `logo.png`)
and add `<img src="assets/logo.png">` at the top of `../README.md`. Not required, and
the README does not depend on it.
